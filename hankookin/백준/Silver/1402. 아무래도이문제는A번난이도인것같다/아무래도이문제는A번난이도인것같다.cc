#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int T, A, B;
    cin >> T >> A >> B;
    
    for (int i=0; i<T; i++) {
        cout << "yes" << "\n";
    }

    return 0;
}