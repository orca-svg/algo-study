name: Weekly Dashboard

on:
  push:
    branches:
      - main

  workflow_dispatch:

  schedule:
    - cron: '0 15 * * 0'

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: weekly-dashboard
  cancel-in-progress: true

jobs:
  dashboard:
    # 특정 레포에서만 실행
    if: github.repository == 'algo-gongbu/algo-study'
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: Debug Workflow Context
        run: |
          echo "===== GITHUB CONTEXT ====="
          echo "event_name=${{ github.event_name }}"
          echo "ref=${{ github.ref }}"
          echo "repository=${{ github.repository }}"
          echo "actor=${{ github.actor }}"

      - name: Generate Weekly Stats
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require("fs");
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            const now = new Date();
            const kstNow = new Date(now.toLocaleString("en-US", { timeZone: "Asia/Seoul" }));
            const day = kstNow.getDay();

            let mondayDate = new Date(kstNow);
            if (day === 0) {
              mondayDate.setDate(mondayDate.getDate() - 6);
            } else {
              mondayDate.setDate(mondayDate.getDate() - (day - 1));
            }
            mondayDate.setHours(0, 0, 0, 0);
            const mondayUTC = new Date(mondayDate.toISOString());

            const pulls = await github.paginate(github.rest.pulls.list, {
              owner,
              repo,
              state: "closed",
              per_page: 100
            });

            const weekly = pulls.filter(pr => pr.merged_at && new Date(pr.merged_at) >= mondayUTC);

            const counts = {};
            weekly.forEach(pr => {
              const author = pr.user.login;
              counts[author] = (counts[author] || 0) + 1;
            });

            let table = `📅 기준 주간 시작: ${mondayDate.toISOString().slice(0, 10)} (월요일 00:00 KST)\n\n`;
            table += "| Member | Count | Status |\n|--------|-------|--------|\n";

            const authors = Object.keys(counts).sort();
            if (authors.length === 0) {
              table += "| - | 0 | ❌ |\n";
            } else {
              authors.forEach(user => {
                const count = counts[user];
                const status = count >= 5 ? "✅" : "❌";
                table += `| ${user} | ${count} | ${status} |\n`;
              });
            }

            fs.writeFileSync("dashboard_table.txt", table);

      - name: Inject Dashboard into README
        run: |
          python <<'PY'
          from pathlib import Path
          import sys

          readme = Path("README.md")
          table = Path("dashboard_table.txt")

          if not readme.exists() or not table.exists():
              print("Required files missing.", file=sys.stderr)
              sys.exit(1)

          content = readme.read_text(encoding="utf-8")
          dashboard = table.read_text(encoding="utf-8").strip()

          start_marker = "<" + "!-- WEEKLY_DASHBOARD_START --" + ">"
          end_marker = "<" + "!-- WEEKLY_DASHBOARD_END --" + ">"

          if start_marker not in content or end_marker not in content:
              print(f"Error: Cannot find markers in README.md", file=sys.stderr)
              sys.exit(1)

          try:
              before = content.split(start_marker)[0]
              after = content.split(end_marker)[1]
          except ValueError as e:
              print(f"Split failed: {e}", file=sys.stderr)
              sys.exit(1)

          updated_content = before + start_marker + "\n" + dashboard + "\n" + end_marker + after
          readme.write_text(updated_content, encoding="utf-8")
          print("README updated successfully!")
          PY

      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add README.md
          if git diff --staged --quiet; then
            echo "No changes detected"
            exit 0
          fi

          git commit -m "📊 weekly dashboard update [skip ci]"
          git pull --rebase origin main

          for i in 1 2 3
          do
            git push origin HEAD:main && break
            echo "Push failed. Retry $i/3"
            if [ $i -eq 3 ]; then exit 1; fi
            sleep 5
          done
